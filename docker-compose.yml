services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: surveyseal
      POSTGRES_PASSWORD: surveyseal
      POSTGRES_DB: surveyseal
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
